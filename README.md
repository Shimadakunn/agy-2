# AGENTERM
The next generation of terminal for work people

pnpm i
pnpm run dev

cp .env.exemple
then set the env files

go to `chrome://inspect/#remote-debugging` and toggle remote debugging
check the port and replace it in .env
